{
  "name": "playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "3D retro playground lobby — equipment-to-minigame selector (Spec 01)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  }
}
